[package]
name = "colorscheme"
version = "0.1.0"
edition = "2024"
readme = "README.md"
description = "CLI tool to generate CSS colorschemes"

[dependencies]
clap = { version = "4.5.29", features = ["derive"] }
colorsys = "0.6.7"
css-named-colors = "1.0.1"
